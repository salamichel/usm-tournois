<% if (flashMessage) { %>
    <div class="p-4 mb-4 text-sm rounded-lg <%= flashMessage.type === 'success' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>" role="alert">
        <%= flashMessage.message %>
    </div>
<% } %>
