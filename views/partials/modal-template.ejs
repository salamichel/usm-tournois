<div id="<%= modalId %>" class="fixed inset-0 bg-gray-900 bg-opacity-75 flex items-center justify-center z-50 hidden p-4">
    <div class="glass-card rounded-lg p-8 w-full max-w-md mx-auto relative">
        <button id="<%= closeButtonId %>" class="absolute top-4 right-4 text-gray-400 hover:text-white text-3xl leading-none">&times;</button>
        <h3 class="text-2xl font-bold text-white mb-6 text-center"><%= title %></h3>
        <%- content %>
    </div>
</div>
