<%- include('../../partials/header') %>

<div class="container mt-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/admin/dashboard">Tableau de bord</a></li>
            <li class="breadcrumb-item"><a href="/admin/tournaments">Tournois</a></li>
            <li class="breadcrumb-item"><a href="/admin/tournaments/<%= tournament.id %>/edit"><%= tournament.name %></a></li>
            <li class="breadcrumb-item active" aria-current="page">Matchs Garantis</li>
        </ol>
    </nav>

    <h1 class="mb-4">Matchs Garantis pour le Tournoi: <%= tournament.name %></h1>

    <div class="card mb-4">
        <div class="card-header">
            Nombre total de matchs garantis
        </div>
        <div class="card-body">
            <h2 class="card-title display-4"><%= guaranteedMatches %></h2>
            <p class="card-text">Ce nombre représente une estimation des matchs qui seront joués au minimum, en fonction de la configuration actuelle du tournoi.</p>
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header">
            Paramètres du tournoi utilisés pour le calcul
        </div>
        <div class="card-body">
            <ul class="list-group list-group-flush">
                <li class="list-group-item"><strong>Nombre maximum d'équipes:</strong> <%= tournament.maxTeams %></li>
                <li class="list-group-item"><strong>Nombre maximum d'équipes par poule:</strong> <%= tournament.maxTeamsPerPool %></li>
                <li class="list-group-item"><strong>Format des matchs de poule:</strong> <%= tournament.matchFormat === 'aller' ? 'Aller simple (chaque équipe joue une fois contre les autres)' : tournament.matchFormat %></li>
                <li class="list-group-item"><strong>Phase d'élimination activée:</strong> <%= tournament.eliminationPhaseEnabled ? 'Oui' : 'Non' %></li>
                <% if (tournament.eliminationPhaseEnabled) { %>
                    <li class="list-group-item"><strong>Équipes qualifiées par poule:</strong> <%= tournament.teamsQualifiedPerPool %></li>
                <% } %>
            </ul>
        </div>
    </div>

    <a href="/admin/tournaments/<%= tournament.id %>/edit" class="btn btn-secondary">Retour au tournoi</a>
</div>

<%- include('../../partials/footer') %>
