import { Request, Response } from 'express';

// TODO: Implement full match controller logic

export const submitScores = async (req: Request, res: Response) => {
  res.json({ success: true, message: 'Submit scores - TODO' });
};
